# Production environment template for the URS frontend.
# Set this as an environment variable in your static-hosting platform
# (Cloudflare Pages / Vercel / Netlify build settings) - do not commit a
# real .env.production file.

VITE_API_URL=https://api.<yourdomain>.com/api
